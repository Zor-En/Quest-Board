{
    "version": 2,
    "builds": [
      {
        "src": "client/(.*)",
        "use": "@vercel/static",
        "config": {
          "public": "client"
        }
      },
      {
        "src": "server/server.js",
        "use": "@vercel/node"
      }
    ],
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "server/server.js"
      },
      {
        "src": "/(.*)",
        "dest": "/client/$1"
      }
    ]
  }